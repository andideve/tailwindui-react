import { ButtonOptions } from './types';

const defaults: Required<ButtonOptions> = {
  variant: 'filled',
  size: 'md',
  rounded: 'md',
  iconLeft: null,
  iconRight: null,
};

export default defaults;
