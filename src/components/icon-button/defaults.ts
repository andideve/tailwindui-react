import btnDefaults from '../button/defaults';
import { IconButtonOptions } from './types';

const defaults: Required<IconButtonOptions> = {
  variant: btnDefaults.variant,
  size: btnDefaults.size,
  rounded: btnDefaults.rounded,
};

export default defaults;
